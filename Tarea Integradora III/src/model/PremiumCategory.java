package model;

// This code is defining an enumeration called `PremiumCategory` with three possible values: `PLATA`,
// `ORO`, and `DIAMANTE`. An enumeration is a special type in Java that represents a fixed set of
// constants. In this case, `PremiumCategory` is used to represent different levels of premium
// membership.
public enum PremiumCategory {
    PLATA,ORO,DIAMANTE;
}
